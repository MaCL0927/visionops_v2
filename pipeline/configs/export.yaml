# 分类模型 ONNX 导出配置
onnx:
  opset_version: 12
  input_size: [1, 3, 224, 224]
  dynamic_axes: null
  simplify: true

# YOLO 导出参数，仅 detection 分支使用
# 分类分支不会使用这里
yolo_export:
  format: onnx
  simplify: true
  opset: 12
  imgsz: 640
  half: false
  dynamic: false